---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: 1.17.2

inputs:
  - name: dp-frontend-area-profiles

outputs:
  - name: build

caches:
  - path: go/

run:
  path: dp-frontend-area-profiles/ci/scripts/build.sh
