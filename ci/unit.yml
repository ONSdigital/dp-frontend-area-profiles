---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: 1.16

inputs:
  - name: dp-frontend-area-profiles-controller

caches:
  - path: go/

run:
  path: dp-frontend-area-profiles-controller/ci/scripts/unit.sh
